logging: 
  level: Info
  format: text
databaseType: yaml
users:
- username: user # Username
  password: "XohImNooBHFR0OVvjcYpJ3NgPQ1qq73WKhHvch0VQtg=" # Hashed password from ./kvdb -generate=password
  hosts:
  - "::1"
  - 127.0.0.1
  permissionsset:
  - namespaces:
    - "*"
    permissions:
      read: true
      write: true
      list: true
- username: test # Username
  password: "n3NeDfmh3ccCvwoae4MDP59xU6AMKd6CztrcmVcomwU=" # Hashed password from ./kvdb -generate=testpassword
  hosts:
  - 172.17.0.6
  permissionsset:
  - namespaces:
    - "hello"
    permissions:
      read: true
      write: true
      list: true
  - namespaces:
    - "world"
    permissions:
      read: true
      write: false
      list: false
publicReadableNamespaces:
- public
trustedProxies: # List of hosts that are trusted reading HostHeadders for. If request not from list only ip origin will be used
- 172.17.0.1
redis:
  address: "127.0.0.1:6379"
  # envVariableName: # Set if different from KVDB_REDIS_PASSWORD
mysql:
  address: "127.0.0.1:3306"
  # username: "kvdb"
  # databaseName: "kvdb"
  # systemTableName: "kvdb"
  # envVariableName: # Set if different from KVDB_MYSQL_PASSWORD
postgres:
  address: "127.0.0.1:5432"  # Or your Kubernetes service address
  # username: "kvdb"
  # databaseName: "kvdb"
  # systemTableName: "kvdb"
  # envVariableName: # Set if different from KVDB_POSTGRES_PASSWORD
prometheus:
  enabled: true # enable /system/metrics prometheus endpoint (for all users and hosts)
  # endpoint: # Set if different from metrics

# oidc:
  # enabled: false # Enable OIDC authentication
  # providerURL: http://127.0.0.1:9096/.well-known/openid-configuration # OIDC discovery URL
  # clientID: kvdb # OAuth2 client ID registered with provider
  # clientSecret: secret # OAuth2 client secret
  # envVariableName: # Set if different from KVDB_OIDC_CLIENTSECRET
  # redirectURL: http://127.0.0.1:8080/oidc/callback # Redirect URL after OIDC login
  # scopes: # OIDC scopes
    # - openid
    # - profile
    # - email
  # tokenTTL: 60 # Session token TTL in minutes
  # disableBasicAuth: false # Disable basic auth when OIDC is enabled
  # cookieName: kvdb_oidc_session # Cookie name for OIDC session JWT
  # cookieDomain: # Optional cookie domain
